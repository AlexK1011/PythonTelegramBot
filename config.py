from format_time import format_time

delay_for_channel = 3
monitor_interval = 60
default_delay = 3600
default_min_forward_rate = 1
system_prompt_for_russian_market = """### Роль  
Ты — профессиональный редактор новостных Telegram-каналов. Перерабатывай полученные посты, сохраняя суть, но делая текст уникальным и адаптированным под стиль целевого канала.

### Стилевые ориентиры (ВАЖНО)
Основывайся на этих примерах, но не копируй их слепо:

1. ||%HEADER%||России снова грозит рост цен на бензин||%HEADER%||  
   В августе и сентябре... экспорта крайне малы... (Reuters)  
   «Будет очень сложно...» — сообщил источник...  

2. ||%HEADER%||В России запретят держать дома медведей||%HEADER%||  
   С 1 сентября... диких хищников...  
   Под запрет... десятки видов...  

3. ||%HEADER%||Россияне бросились скупать WiFi-роутеры...||%HEADER%||  
   Спрос... вырос вдвое... («Известия»)  
   Покупает... малый и средний бизнес...  

4. ||%HEADER%||С 1 августа россияне смогут... спам-звонки||%HEADER%||  
   Операторы обязаны...  
   А если... через Госуслуги...  

5. ||%HEADER%||Казахстан попросил Турцию... в обход России||%HEADER%||  
   Переговоры... увеличения экспорта... (Reuters)  
   Казахстан увеличил... на 13%...

**Ключевые черты стиля:**  
- Заголовки: лаконичные, без кликбейта (5-10 слов)  
- Текст: 1-3 абзаца, факты > мнений  
- Источники: в скобках (Reuters, «Известия»)  
- Экспрессия: умеренная («бросились скупать»)  
- Формат: строго `||%HEADER%||Заголовок||%HEADER%||Текст`  

### Правила обработки  
1. **Уникализация**  
   - Переписывай предложения (синонимы + новая структура)  
   - Цитаты → косвенная речь («Источник сообщил, что...»)  
   - Сохраняй ключевые термины (Госуслуги, БТД, НПЗ)  

2. **Адаптация**  
   - Тон: нейтральный с элементами экспрессии где уместно  
   - Объём: без «воды», но можно длиннее примеров при необходимости  
   - Числа/даты: формат свободный (1 августа / 01.08)  

Весь пост ≤ 4096 символов 
очень важно чтобы ты не добавлял сам в текст какой либо markdown код, т.к.программа не сможет его обработать и он не будет форматировать текст.

### Формат вывода  
ТОЛЬКО так:  
||%HEADER%||[Заголовок]||%HEADER%||
[Основная часть поста]"""
modes = ['delayed_check', 'periodic_collection']
change_mode_text = """Выберите режим работы.

1. <b>Отложенная проверка</b> - бот ждет указанное время после публикации поста, затем проверяет процент репостов. Если процент превышает порог - обрабатывает и отправляет пост.

2. <b>Периодический сбор</b> - бот через заданные интервалы анализирует все новые посты, выбирает топ-N публикаций с самым высоким процентом репостов и отправляет их вам.

выберите режим:"""


def get_settings_text(settings):

    mode = settings.get("mode", 'delayed_check')
    delay_text = format_time(settings.get("delay", 3600))
    reposts_percent = settings.get("min_forward_rate", 1.0)

    reposts_text = f"{reposts_percent}%" if reposts_percent == int(reposts_percent) \
        else f"{round(reposts_percent, 2)}%"
    ai_enabled = int(settings.get("ai_enabled", 0))
    interval = format_time(settings.get("interval", 3600))
    number_of_posts = settings.get("number_of_posts", 5)

    text_mode1 = """Текущий режим: <b>отложенная проверка</b>
    
1. установить <b>Задержку</b> - время ожидания перед анализом статистики поста. Бот будет ждать указанное время, а затем проверять процент репостов. Это нужно для сбора полной статистики поста.

2. установить <b>Минимальный процент репостов</b> - пороговое значение доли репостов относительно просмотров. Пост будет отправлен вам только если процент репостов достигнет или превысит это значение.

3. нужно ли использовать <b>ИИ</b> для обработки поста."""
    text_mode2 = """Текущий режим: <b>периодический сбор</b>

    1. установить <b>Интервал проверки</b> - как часто бот будет анализировать новые посты. Бот соберёт все публикации за указанный период, проверит их статистику и выберет самые популярные.

    2. установить <b>Размер топа</b> - количество постов с наивысшим процентом репостов, которые будут отобраны и отправлены вам. Например, топ-5 или топ-10 самых репостнутых публикаций.

    3. нужно ли использовать <b>ИИ</b> для обработки отобранных постов."""
    settings_mode1 = f"""• Задержка: <b>{delay_text}</b>
• Минимальный процент репостов: <b>{reposts_text}</b>
• ИИ: <b>{'Вкл.' if bool(ai_enabled) else 'Выкл.'}</b>
{"• системный промпт: " + settings.get('system_prompt', '') if bool(ai_enabled) else ''}"""

    settings_mode2 = f"""• Интервал проверки: <b>{interval}</b>
• Размер топа: <b>{number_of_posts}</b>
• ИИ: <b>{'Вкл.' if bool(ai_enabled) else 'Выкл.'}</b>
{"• системный промпт: " + settings.get('system_prompt', '') if bool(ai_enabled) else ''}"""


    return f"""⚙️ Настройки

{text_mode1 if mode == 'delayed_check' else text_mode2}

Нажмите на нужную настройку для изменения.

📋 <b>Текущие настройки:</b>
{settings_mode1 if mode == 'delayed_check' else settings_mode2}
"""


def get_text_from(message_link, channel_title, forward_rate, forwards):
    return (f"<a href='{message_link}'>исходный пост</a> в {channel_title}\n"
            f"Процент репостов: <b>{round(forward_rate, 2)}%</b>\nВсего поделились: <b>{forwards} раз</b>")


rate_limit = 10
time_window = 10
max_tokens = 33000
max_retries = 5
base_delay = 1
max_channels = 40
